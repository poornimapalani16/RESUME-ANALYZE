const score = Math.floor(Math.random() * 30) + 70;

const response = {
  score,
  strengths: ["HTML", "CSS", "React"],
  missing_skills: ["Node.js", "MongoDB"],
  suggestions: ["Improve projects", "Add keywords"]
};
